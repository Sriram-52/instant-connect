import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

export default function MessagePanel() {
  return (
    <View>
      <Text>This is a message panel. It is used to display messages to the user.</Text>
    </View>
  );
}
